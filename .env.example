# No environment variable is required for CSV-backed research experiments.
# Database-backed callers create an explicit SQLAlchemy engine and pass it to
# scripts.db; the research layer never discovers sibling collectors or secrets.
