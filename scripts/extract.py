"""Implement the source-specific extractor here.

The function must return an object with attributes: catalog, observations,
releases, snapshots, min_lag_days, max_lag_days and inferred_lag_days.
"""
from __future__ import annotations

def collect():
    raise NotImplementedError("Replace scripts/extract.py with the source-specific collector")
