# Versioned experiment outputs

Commit compact ranking CSVs, JSON run manifests and short Markdown summaries
here. Do not commit production databases, publisher workbooks or raw snapshots.

No empirical result is checked in until the persisted predictor and target
contracts are supplied to the engine. This prevents synthetic test data from
being mistaken for economic evidence.
